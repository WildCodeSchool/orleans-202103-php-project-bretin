<form method="post" action="{{ path('testimony_delete', {'id': testimony.id}) }}" onsubmit="return confirm('Confirmez-vous la suppression?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ testimony.id) }}">
    <button class="btn btn-primary">Supprimer</button>
</form>
