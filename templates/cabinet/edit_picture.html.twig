{% extends 'base.html.twig' %}

{% block title %}Modifier une photo du cabinet{% endblock %}

{% block body %}
    <header class="container-fluid m-3 p-2"></header>
    <main class="container-fluid">
        <div>
            <div class = "fw-bold text-center">
                <p class = "titre">Changer la photo</p>
            </div>
            {{ form_start(form) }}
                <div class="d-flex justify-content-center m-2">
                    <div class="row col-lg-6 col-md-8 mb-4 fw-bold">
                        {{ form_label(form.officeFile, "Mettre à jour vos visuels") }}
                        {{ form_widget(form.officeFile) }}
                        {{ form_errors(form.officeFile) }}
                    <div>
                </div>
            {{ form_end(form) }}
                <button class="btn btn-primary m-2 col-3 px-1">{{ button_label|default('Enregistrer') }}</button>
                {% if picture.url is not empty %}
                  {{ include('cabinet/_delete_pic.html.twig') }}
                {% endif %}
        </div>
    </main>
{% endblock %}
