{% extends 'base.html.twig' %}

{% block title %}Office{% endblock %}

{% block body %}
    <h1>Office</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ office.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ office.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ office.address }}</td>
            </tr>
            <tr>
                <th>Zipcode</th>
                <td>{{ office.zipcode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ office.city }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ office.phone }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ office.mail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cabinet_index') }}">back to list</a>

    <a href="{{ path('cabinet_edit', {'id': office.id}) }}">edit</a>

    {{ include('cabinet/_delete_form.html.twig') }}
{% endblock %}
