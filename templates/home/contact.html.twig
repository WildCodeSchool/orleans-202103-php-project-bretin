{% extends 'layout.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}

<div class = "container-fluid">
  <div class="row">
    <div class="col-lg-4 p-3">
     <p>Si vous êtes un PARTICULIER</p>
    </div>
  </div>
  <div class="container">
    <div class = "row">
      {% set tel= 0 %}
      {% set mail = 0 %}
    </div>
        {% for office in offices %}
        {% set tel= office.phone %}
        {% set mail= office.mail %}
        <div class="card">
          <div class="card-header">
               {{ office.name }}
          </div>
            <div class="card-body">
              <h5 class="card-title">  {{ office.address }} - {{ office.zipcode }} </h5>
              <p class="card-text"> {{ office.phone }}</p>
            </div>
          </div>
            {% endfor %}
            <div class="card-body">
              <p class="card-text"><strong>J'interviens uniquement sur RDV</strong>.<br>
               Je vous invite à me joindre par téléphone au <strong> {{ tel }}</strong> ou mail <strong>{{ mail }} </strong> pour prendre rendez-vous ou obtenir des renseignements.
               <br>Les séances sont d'une durée de 50 à 60 minutes.</p>
            </div>
  </div>
</div>

{% endblock %}
